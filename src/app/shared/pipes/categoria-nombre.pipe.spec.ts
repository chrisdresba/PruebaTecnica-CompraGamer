import { CategoriaNombrePipe } from './categoria-nombre.pipe';

describe('CategoriaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
